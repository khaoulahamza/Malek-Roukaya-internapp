name: Wind App CI/CD

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    uses: khaoulahamza/devops-templates/.github/workflows/deploy.yml@main
    secrets:
      NEXUS_USERNAME: ${{ secrets.NEXUS_USERNAME }}
      NEXUS_PASSWORD: ${{ secrets.NEXUS_PASSWORD }}
      NEXUS_REGISTRY: ${{ secrets.NEXUS_REGISTRY }}
